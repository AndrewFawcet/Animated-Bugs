package application;

import javafx.scene.paint.Color;
import javafx.scene.shape.Circle;

public class Dead extends Circle{
	
	public Dead(double x, double y, double z) {

		super(x, y, z);

	}

	//used to replace dead bugs and plants, as there are no methods, 
	//needs to be an inert fixed object
	
}
